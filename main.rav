import stdlib/sysio.rav

fn main() {
	string msg = "Hello world!";
	print.call(msg);

	int[] nums = [0, 2, 3, 7, 6, 5, 4];
	int sum = nums.2 + nums.5;
	print.call(sum);

	int x = 0;
	
	while (x <= 5) {
		print.call(x);
		x = x + 1;
	}
}